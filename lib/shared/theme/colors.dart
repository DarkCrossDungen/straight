import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Light ──
  static const lightBg = Color(0xFFFFFFFF);
  static const lightFg = Color(0xFF000000);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightMuted = Color(0xFFF5F5F5);
  static const lightMutedFg = Color(0xFF666666);
  static const lightBorder = Color(0xFF000000);
  static const lightDivider = Color(0xFF000000);

  // ── Dark ──
  static const darkBg = Color(0xFF000000);
  static const darkFg = Color(0xFFFFFFFF);
  static const darkCard = Color(0xFF0A0A0A);
  static const darkMuted = Color(0xFF1F1F1F);
  static const darkMutedFg = Color(0xFFB3B3B3);
  static const darkBorder = Color(0xFFFFFFFF);
  static const darkDivider = Color(0xFFFFFFFF);

  // ── Brand ──
  static const primaryLight = Color(0xFFDB0000);
  static const primaryDark = Color(0xFFFF0000);
  static const accentLight = Color(0xFFFFD700);
  static const accentDark = Color(0xFFFFCC00);

  // ── Semantic ──
  static const success = Color(0xFF2E7D32);
  static const warning = Color(0xFFF57F17);
  static const error = Color(0xFFD50000);
  static const info = Color(0xFF1565C0);

  // ── Waveform ──
  static const waveformIdle = Color(0xFF333333);
  static const waveformActive = Color(0xFFFF0000);
  static const waveformDark = Color(0xFFCCCCCC);
}
